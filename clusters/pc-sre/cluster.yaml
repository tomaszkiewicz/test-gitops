amcm:
  cluster:
    disableAutoSync: true

  kustomizeApplications:
    coredns-eks:
      path: addons/coredns-eks

  helmApplications:
    external-dns:
      addonFile: ../../addons/external-dns.yaml
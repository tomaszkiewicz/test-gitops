stage1: &config
  cluster:
    name: test
    repo: https://github.com/tomaszkiewicz/test-gitops.git
    repoPathPrefix: helm

  helmApplications:
    external-dns:
      addonFile: ../../addons/external-dns.yaml

stage2: *config
stage3: *config